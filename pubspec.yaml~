name: flutter_dittofeed
description: "Plugin for Dittofeed"
version: 0.0.1
homepage:

environment:
  sdk: ^3.7.2
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  http: ^1.3.0
  uuid: ^4.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

  test: ^1.25.15
  mockito: ^5.4.5
  http_mock_adapter: ^0.6.1

# The following section is specific to Flutter packages.
flutter:
